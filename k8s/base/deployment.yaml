apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-converter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: currency-converter
  template:
    metadata:
      labels:
        app: currency-converter
    spec:
      containers:
      - name: currency-converter
        image: yasminemarzouk/apptest:2e08612
        ports:
        - containerPort: 5555
        env:
        - name: APIKEY
          valueFrom:
            secretKeyRef:
              name: api-keys
              key: APIKEY
        
        - name: APIHOST
          value: "currency-exchange.p.rapidapi.com"
        - name: FLASK_ENV
          value: "development"
